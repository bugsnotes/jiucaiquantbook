# 第二十三章：实盘部署指南

🎯 **适合人群**：准备实盘的用户 | ⏱️ **阅读时间**：1小时

## 本章导读

策略回测成功后，实盘部署是最关键的一步。本章提供完整的实盘部署检查清单和最佳实践。

**本章内容**：
- ✅ 23.1 实盘前的准备
- 🏗️ 23.2 部署架构设计
- 💰 23.3 从小资金开始

---

## 23.1 实盘前的准备

### ● 23.1.1 策略最终检查清单

```markdown
# 实盘部署检查清单

## 回测验证 ✓
- [ ] 样本内回测通过
- [ ] 样本外回测通过
- [ ] 至少包含一个完整牛熊周期
- [ ] 夏普比率 > 1.0
- [ ] 最大回撤可接受（< -20%）
- [ ] 交易成本已充分考虑

## 代码质量 ✓
- [ ] 代码审查完成
- [ ] 单元测试覆盖率 > 80%
- [ ] 无未来函数
- [ ] 日志记录完善
- [ ] 异常处理健全
- [ ] 性能优化完成

## 风控机制 ✓
- [ ] 止损机制已测试
- [ ] 仓位限制已设置
- [ ] 回撤控制已配置
- [ ] 应急止损已准备
- [ ] 风险预警已部署

## 交易接口 ✓
- [ ] 接口稳定性测试通过
- [ ] 下单速度测试通过
- [ ] 撤单功能测试通过
- [ ] 异常处理测试通过
- [ ] 模拟盘测试通过（至少1个月）

## 监控系统 ✓
- [ ] 实时监控面板已部署
- [ ] 告警系统已配置
- [ ] 日志系统已部署
- [ ] 每日报告自动生成

## 数据准备 ✓
- [ ] 数据源稳定可靠
- [ ] 数据更新及时
- [ ] 数据备份方案已部署
- [ ] 数据异常处理已测试

## 其他 ✓
- [ ] 应急预案已准备
- [ ] 联系人列表已更新
- [ ] 小资金测试计划已制定
- [ ] 文档完整齐全
```

### ● 23.1.2 风控参数设置

```python
# 风控配置示例
RISK_CONTROL_CONFIG = {
    # 仓位控制
    'max_total_position': 0.95,      # 最大总仓位95%
    'max_single_position': 0.05,     # 单只股票最大5%
    'max_industry_position': 0.20,   # 单行业最大20%

    # 回撤控制
    'max_drawdown_warning': -0.10,   # 回撤10%预警
    'max_drawdown_reduce': -0.15,    # 回撤15%减仓
    'max_drawdown_stop': -0.20,      # 回撤20%停止

    # 交易控制
    'max_daily_trades': 50,          # 每日最大交易次数
    'max_daily_turnover': 0.50,      # 每日最大换手50%
    'min_order_interval': 60,        # 最小下单间隔（秒）

    # 止损止盈
    'stop_loss_pct': -0.05,          # 个股止损-5%
    'take_profit_pct': 0.10,         # 个股止盈10%

    # 其他
    'forbid_st_stocks': True,        # 禁止ST股票
    'forbid_suspended': True,        # 禁止停牌股票
}
```

---

## 23.2 部署架构设计

### ● 23.2.1 单机部署方案

```
┌─────────────────────────────────┐
│       单机部署架构              │
├─────────────────────────────────┤
│                                 │
│  ┌───────────────────────────┐ │
│  │   主程序（main.py）      │ │
│  │  - 策略逻辑              │ │
│  │  - 数据获取              │ │
│  │  - 订单管理              │ │
│  └───────────────────────────┘ │
│            ↓                    │
│  ┌───────────────────────────┐ │
│  │   数据存储（本地）       │ │
│  │  - SQLite/HDF5           │ │
│  └───────────────────────────┘ │
│            ↓                    │
│  ┌───────────────────────────┐ │
│  │   交易接口               │ │
│  │  - QMT/vnpy              │ │
│  └───────────────────────────┘ │
│            ↓                    │
│  ┌───────────────────────────┐ │
│  │   监控与日志             │ │
│  │  - 文本日志              │ │
│  │  - 钉钉告警              │ │
│  └───────────────────────────┘ │
│                                 │
└─────────────────────────────────┘

优点：简单、成本低
缺点：单点故障风险
适用：个人交易者、小资金
```

### ● 23.2.2 分布式架构（可选）

```
┌─────────────────────────────────────┐
│        分布式部署架构              │
├─────────────────────────────────────┤
│                                     │
│  ┌──────────┐  ┌──────────┐       │
│  │ 策略服务1│  │ 策略服务2│       │
│  └─────┬────┘  └─────┬────┘       │
│        │              │             │
│        └──────┬───────┘             │
│               ↓                     │
│     ┌─────────────────┐            │
│     │   消息队列      │            │
│     │   (Redis/RabbitMQ)           │
│     └─────────────────┘            │
│               ↓                     │
│     ┌─────────────────┐            │
│     │   交易服务      │            │
│     └─────────────────┘            │
│               ↓                     │
│     ┌─────────────────┐            │
│     │   数据库集群    │            │
│     │   (MySQL/Redis) │            │
│     └─────────────────┘            │
│                                     │
└─────────────────────────────────────┘

优点：高可用、可扩展
缺点：复杂、成本高
适用：专业团队、大资金
```

---

## 23.3 从小资金开始

### ● 23.3.1 三阶段实盘策略

```python
"""
三阶段实盘验证策略
"""

class ThreeStageDeployment:
    def __init__(self, strategy):
        self.strategy = strategy
        self.stage = 1

    # 第1阶段：1-3个月，1万元
    def stage_1_pilot(self):
        """
        第一阶段：试点测试

        目标：
        - 验证策略在真实市场的表现
        - 检查执行偏差
        - 发现潜在问题

        资金：1万元
        预期：可能亏损，但获得宝贵经验
        """
        print("=" * 60)
        print("第1阶段：试点测试（1万元）")
        print("=" * 60)

        config = {
            'capital': 10000,
            'max_position': 0.80,
            'max_single_stock': 0.10,
            'stop_loss': -0.10,
        }

        # 重点监控指标
        monitor_metrics = [
            '执行偏差',
            '滑点成本',
            '实际换手率 vs 回测换手率',
            '实际夏普 vs 回测夏普',
        ]

        # 每周复盘
        weekly_review = [
            '交易记录回顾',
            '偏差原因分析',
            '策略参数是否需要调整',
            '风控是否有效',
        ]

        return config

    # 第2阶段：3-6个月，5-10万元
    def stage_2_validation(self):
        """
        第二阶段：验证稳定性

        前提：第1阶段表现良好（夏普>0.5）

        目标：
        - 验证策略的稳定性
        - 确认容量不是问题
        - 优化执行细节

        资金：5-10万元
        """
        print("=" * 60)
        print("第2阶段：验证稳定性（5-10万元）")
        print("=" * 60)

        config = {
            'capital': 100000,
            'max_position': 0.90,
            'max_single_stock': 0.08,
            'stop_loss': -0.12,
        }

        # 关键检查点
        checkpoints = {
            '月度夏普': '> 1.0',
            '月度最大回撤': '< -15%',
            '月度胜率': '> 50%',
            '执行偏差': '< 5%',
        }

        return config

    # 第3阶段：6-12个月，逐步放大
    def stage_3_scale_up(self):
        """
        第三阶段：规模化

        前提：第2阶段表现符合预期

        目标：
        - 达到策略容量上限
        - 实现稳定盈利

        资金：逐步增加到目标规模
        增速：每月增加10-20%
        """
        print("=" * 60)
        print("第3阶段：规模化（逐步增加）")
        print("=" * 60)

        # 资金增长计划
        growth_plan = {
            'month_1': 100000,
            'month_2': 120000,  # +20%
            'month_3': 144000,  # +20%
            'month_4': 173000,  # +20%
            'month_5': 200000,  # +15%
            'month_6': 230000,  # +15%
        }

        # 持续监控
        continuous_monitoring = [
            '策略收益与资金规模的关系',
            '流动性影响分析',
            '是否出现容量瓶颈',
            '竞争环境变化',
        ]

        return growth_plan

    def should_stop(self, metrics):
        """
        判断是否应该停止

        停止条件：
        1. 连续3个月亏损
        2. 最大回撤 > -25%
        3. 夏普比率 < 0.3 持续3个月
        4. 执行偏差持续 > 10%
        """
        stop_signals = []

        if metrics['consecutive_loss_months'] >= 3:
            stop_signals.append("连续3个月亏损")

        if metrics['max_drawdown'] < -0.25:
            stop_signals.append("最大回撤超过-25%")

        if metrics['sharpe_ratio'] < 0.3 and metrics['low_sharpe_months'] >= 3:
            stop_signals.append("夏普比率持续低于0.3")

        if metrics['execution_deviation'] > 0.10:
            stop_signals.append("执行偏差持续>10%")

        if stop_signals:
            print("\n⚠️ 触发停止条件:")
            for signal in stop_signals:
                print(f"  - {signal}")
            return True

        return False
```

### ● 23.3.2 实盘日志示例

```python
import logging
from datetime import datetime

# 配置日志
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler(f'trading_{datetime.now().strftime("%Y%m%d")}.log'),
        logging.StreamHandler()
    ]
)

logger = logging.getLogger('TradingBot')

# 记录关键事件
logger.info("=" * 60)
logger.info("实盘交易开始")
logger.info("=" * 60)
logger.info(f"初始资金: ¥100,000")
logger.info(f"策略: 双均线策略 v1.0")
logger.info("=" * 60)

# 交易日志
logger.info("生成交易信号: 买入 600000.SH @ ¥10.50")
logger.info("下单成功: 订单ID=12345")
logger.info("成交回报: 600000.SH, 成交价¥10.52, 成交量1000股")

# 警告日志
logger.warning("当前回撤达到-8%, 接近预警线-10%")

# 错误日志
logger.error("数据获取失败: 连接超时", exc_info=True)
```

---

## 本章小结

实盘部署的关键要点：

### 📚 核心原则

1. **充分准备**
   - 完整的检查清单
   - 严格的测试流程
   - 完善的风控机制

2. **小资金试错**
   - 第1阶段：1万元试点
   - 第2阶段：10万元验证
   - 第3阶段：逐步放大

3. **持续监控**
   - 实时性能追踪
   - 执行偏差分析
   - 及时止损

### ⚠️ 重要提醒

1. 绝不要跳过模拟盘测试
2. 绝不要一开始就投入大额资金
3. 出现异常立即停止，不要侥幸

---

*本章完*
