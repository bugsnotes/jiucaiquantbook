# 第十四章：数据获取与处理

**适合人群**: 有Python基础，想要获取和处理股票数据的投资者
**学习时间**: 2-3天
**目标**: 掌握主流免费数据源的使用，能够获取、清洗、存储股票数据

---

> 💡 **数据是量化交易的基础**：没有数据，再好的策略也只是纸上谈兵。本章教你如何获取高质量的股票数据，并进行专业的处理。

---

## 14.1 数据获取实战

### 数据源全景图

**国内主流数据源对比**：

| 数据源 | 类型 | 数据质量 | 价格 | 适用场景 |
|--------|------|---------|------|---------|
| **Tushare Pro** | 免费+付费 | 中等 | 积分制 | 个人学习、小规模回测 |
| **AKShare** | 免费 | 中等 | 免费 | 快速验证、学习 |
| **Wind** | 付费 | 最高 | 贵 | 机构、专业研究 |
| **Choice** | 付费 | 高 | 较贵 | 机构、专业研究 |

**本章重点**：
- ✅ Tushare Pro（免费，适合入门）
- ✅ AKShare（完全免费，快速上手）

---

### Tushare Pro数据获取

#### 1. 注册与Token获取

**步骤**：

```
1. 访问：https://tushare.pro/register
2. 注册账号（手机号即可）
3. 登录后，进入"个人中心" → "接口TOKEN"
4. 复制你的专属Token
```

**积分说明**：

```
Tushare采用积分制：
- 注册送120积分
- 每日签到+1积分
- 邀请好友+100积分
- 充值获得积分

积分用途：
- 不同接口需要不同积分权限
- 日线数据：120积分即可
- 分钟数据：需要2000+积分
- 财务数据：需要更高积分
```

#### 2. 安装与配置

```bash
# 安装Tushare
pip install tushare

# 如果安装慢，使用国内镜像
pip install tushare -i https://pypi.tuna.tsinghua.edu.cn/simple
```

**基本使用**：

```python
import tushare as ts

# 设置Token（只需设置一次）
ts.set_token('你的Token')

# 初始化pro接口
pro = ts.pro_api()

# 测试：获取贵州茅台的日线数据
df = pro.daily(ts_code='600519.SH', start_date='20240101', end_date='20240131')
print(df.head())
```

输出示例：

```
    ts_code trade_date   open   high    low  close  ...
0  600519.SH   20240131  1680.0 1720.5 1675.0 1710.2  ...
1  600519.SH   20240130  1670.5 1685.0 1665.0 1680.0  ...
```

#### 3. 常用数据接口

**（1）股票日线行情**：

```python
import tushare as ts
import pandas as pd

pro = ts.pro_api('你的Token')

# 获取单只股票日线数据
df = pro.daily(
    ts_code='600519.SH',      # 股票代码
    start_date='20240101',    # 开始日期
    end_date='20240131'       # 结束日期
)

# 数据包含：
# ts_code: 股票代码
# trade_date: 交易日期
# open: 开盘价
# high: 最高价
# low: 最低价
# close: 收盘价
# pre_close: 前收盘价
# change: 涨跌额
# pct_chg: 涨跌幅(%)
# vol: 成交量(手)
# amount: 成交额(千元)

print(df)
```

**（2）获取股票列表**：

```python
# 获取所有A股股票列表
stock_list = pro.stock_basic(
    exchange='',
    list_status='L',  # L=上市 D=退市 P=暂停
    fields='ts_code,symbol,name,area,industry,list_date'
)

print(f"共{len(stock_list)}只股票")
print(stock_list.head())

# 筛选：只要主板股票
main_board = stock_list[stock_list['ts_code'].str.startswith(('600', '601', '603', '000', '001'))]
print(f"主板股票：{len(main_board)}只")
```

**（3）获取指数数据**：

```python
# 获取上证指数日线数据
index_df = pro.index_daily(
    ts_code='000001.SH',  # 上证指数
    start_date='20240101',
    end_date='20240131'
)

print(index_df.head())
```

**（4）批量获取多只股票**：

```python
import time

def get_multiple_stocks(stock_codes, start_date, end_date):
    """批量获取多只股票数据"""
    all_data = []

    for code in stock_codes:
        try:
            df = pro.daily(
                ts_code=code,
                start_date=start_date,
                end_date=end_date
            )
            all_data.append(df)

            # 避免接口限流
            time.sleep(0.3)  # 每次请求间隔0.3秒

        except Exception as e:
            print(f"获取{code}失败: {e}")

    # 合并所有数据
    result = pd.concat(all_data, ignore_index=True)
    return result

# 使用
stock_codes = ['600519.SH', '000858.SZ', '600036.SH']
data = get_multiple_stocks(stock_codes, '20240101', '20240131')
print(data)
```

**（5）获取复权数据**：

```python
# 获取后复权数据
df_hfq = pro.daily(
    ts_code='600519.SH',
    start_date='20200101',
    end_date='20241231',
    adj='hfq'  # hfq=后复权，qfq=前复权，None=不复权
)

print(df_hfq.head())
```

**（6）获取财务数据**：

```python
# 获取利润表数据
income = pro.income(
    ts_code='600519.SH',
    start_date='20200101',
    end_date='20241231',
    fields='ts_code,ann_date,f_ann_date,end_date,revenue,operate_profit,total_profit,n_income'
)

print(income)
```

#### 4. Tushare注意事项

```
⚠️ 接口限流
- 普通用户：每分钟调用次数有限制
- 解决方案：请求之间加延时（time.sleep）

⚠️ 积分不足
- 某些高级接口需要更多积分
- 解决方案：每日签到、邀请好友、充值

⚠️ 数据可能有延迟
- 免费数据可能延迟1天
- 实时数据需要更高积分

✅ 优点
- 免费使用
- 数据覆盖全面
- 接口简单

❌ 缺点
- 有积分限制
- 有调用频率限制
- 数据质量一般
```

#### 5. 完整示例：获取沪深300数据

```python
import tushare as ts
import pandas as pd
from tqdm import tqdm
import time

# 初始化
pro = ts.pro_api('你的Token')

# 获取沪深300成分股
hs300 = pro.index_weight(index_code='000300.SH', start_date='20240101', end_date='20240101')
stock_codes = hs300['con_code'].unique().tolist()

print(f"沪深300成分股：{len(stock_codes)}只")

# 批量获取数据
all_data = []
for code in tqdm(stock_codes):
    try:
        df = pro.daily(
            ts_code=code,
            start_date='20200101',
            end_date='20241231',
            adj='hfq'  # 后复权
        )
        all_data.append(df)
        time.sleep(0.3)  # 避免限流

    except Exception as e:
        print(f"获取{code}失败: {e}")

# 合并数据
result = pd.concat(all_data, ignore_index=True)

# 保存到本地
result.to_csv('hs300_daily_data.csv', index=False)
print(f"数据已保存，共{len(result)}条记录")
```

---

### AKShare使用指南

> 💡 **完全免费**：AKShare是开源项目，所有数据完全免费，非常适合学习和快速验证策略。

#### 1. 安装

```bash
pip install akshare

# 如果安装慢
pip install akshare -i https://pypi.tuna.tsinghua.edu.cn/simple
```

#### 2. 基本使用

**获取股票日线数据**：

```python
import akshare as ak

# 获取单只股票历史数据
df = ak.stock_zh_a_hist(
    symbol='600519',        # 股票代码（不要后缀）
    period='daily',         # daily, weekly, monthly
    start_date='20240101',
    end_date='20240131',
    adjust='hfq'            # 后复权
)

print(df.head())
```

**获取实时行情**：

```python
# 获取沪深A股实时行情
realtime = ak.stock_zh_a_spot_em()

# 筛选：只看沪深300
hs300_realtime = realtime[realtime['代码'].isin(['600519', '000858', '600036'])]
print(hs300_realtime[['代码', '名称', '最新价', '涨跌幅']])
```

**获取指数数据**：

```python
# 上证指数历史数据
sh_index = ak.stock_zh_index_daily(symbol='sh000001')
print(sh_index.tail())
```

**获取股票列表**：

```python
# 获取所有A股列表
stock_list = ak.stock_zh_a_spot_em()
print(f"共{len(stock_list)}只股票")
print(stock_list[['代码', '名称', '最新价', '涨跌幅']].head())
```

**获取财务数据**：

```python
# 获取财务指标
financial = ak.stock_financial_analysis_indicator(symbol='600519')
print(financial.tail())
```

#### 3. AKShare优缺点

```
✅ 优点
- 完全免费
- 数据源丰富（股票、期货、期权、基金等）
- 更新及时
- 无需注册

❌ 缺点
- 数据质量一般（可能有错误）
- 接口不稳定（可能突然变化）
- 无官方技术支持
- 不适合实盘使用

🎯 适用场景
- 学习Python量化
- 快速验证策略想法
- 小规模回测
- 做demo演示
```

---

### 多数据源整合方案

在实际使用中，可以结合多个数据源：

```python
class MultiSourceData:
    """多数据源管理器"""

    def __init__(self, tushare_token):
        # 初始化Tushare
        self.tushare = ts.pro_api(tushare_token)

    def get_daily_data(self, symbol, start_date, end_date):
        """
        获取日线数据（优先使用Tushare，失败则用AKShare）
        """
        try:
            # 优先使用Tushare
            ts_code = self._convert_code(symbol)
            df = self.tushare.daily(
                ts_code=ts_code,
                start_date=start_date.replace('-', ''),
                end_date=end_date.replace('-', '')
            )
            print(f"从Tushare获取{symbol}数据成功")
            return df

        except Exception as e:
            print(f"Tushare获取失败，尝试AKShare: {e}")

            try:
                # 使用AKShare
                df = ak.stock_zh_a_hist(
                    symbol=symbol,
                    start_date=start_date.replace('-', ''),
                    end_date=end_date.replace('-', ''),
                    adjust='hfq'
                )
                print(f"从AKShare获取{symbol}数据成功")
                return df

            except Exception as e2:
                print(f"AKShare也失败: {e2}")
                return None

    def _convert_code(self, symbol):
        """转换股票代码格式"""
        if symbol.startswith('6'):
            return f"{symbol}.SH"
        else:
            return f"{symbol}.SZ"

# 使用
data_manager = MultiSourceData(tushare_token='你的token')
df = data_manager.get_daily_data('600519', '2024-01-01', '2024-01-31')
```

---

## 14.2 数据清洗与预处理

> 💡 **数据质量决定策略质量**：脏数据会导致错误的回测结果。专业的数据清洗是量化交易的必修课。

### 缺失值处理

**常见缺失值情况**：

```
1. 停牌期间：某些日期完全没有数据
2. 新股上市：历史数据不足
3. 数据源问题：某些字段缺失
```

**检查缺失值**：

```python
import pandas as pd

# 检查缺失值
print(df.isnull().sum())

# 查看缺失值比例
print(df.isnull().sum() / len(df))

# 可视化缺失值
import missingno as msno
msno.matrix(df)
```

**处理方法**：

```python
# 方法1：删除含缺失值的行
df_clean = df.dropna()

# 方法2：前向填充（用前一个有效值填充）
df['close'] = df['close'].fillna(method='ffill')

# 方法3：后向填充
df['close'] = df['close'].fillna(method='bfill')

# 方法4：用均值填充
df['close'] = df['close'].fillna(df['close'].mean())

# 方法5：插值法
df['close'] = df['close'].interpolate(method='linear')

# 推荐方案：价格用前向填充，成交量填0
df['close'] = df['close'].fillna(method='ffill')
df['volume'] = df['volume'].fillna(0)
```

### 异常值检测

**常见异常值**：

```
1. 价格异常：突然暴涨暴跌（可能是数据错误）
2. 成交量异常：突然放巨量或为0
3. 逻辑错误：最高价<最低价
```

**检测方法**：

```python
def detect_anomalies(df):
    """检测异常值"""

    # 1. 检查价格逻辑
    invalid_ohlc = df[df['high'] < df['low']]
    if len(invalid_ohlc) > 0:
        print(f"发现{len(invalid_ohlc)}条价格逻辑错误")
        print(invalid_ohlc)

    # 2. 检查极端涨跌幅（单日涨跌超过30%可能是数据错误）
    df['pct_change'] = df['close'].pct_change()
    extreme_change = df[abs(df['pct_change']) > 0.3]
    if len(extreme_change) > 0:
        print(f"发现{len(extreme_change)}条极端涨跌")
        print(extreme_change[['trade_date', 'close', 'pct_change']])

    # 3. 检查成交量为0
    zero_volume = df[df['volume'] == 0]
    if len(zero_volume) > 0:
        print(f"发现{len(zero_volume)}条成交量为0")

    # 4. 使用3-sigma规则检测离群点
    mean = df['close'].mean()
    std = df['close'].std()
    outliers = df[abs(df['close'] - mean) > 3 * std]
    if len(outliers) > 0:
        print(f"发现{len(outliers)}条离群点")

    return df

# 使用
df_checked = detect_anomalies(df)
```

**处理方法**：

```python
# 方法1：删除异常值
df_clean = df[abs(df['pct_change']) <= 0.3]

# 方法2：用中位数替换
median = df['close'].median()
df.loc[abs(df['pct_change']) > 0.3, 'close'] = median

# 方法3：用前一日价格替换（对于明显错误的数据）
def fix_outliers(df):
    """修正异常值"""
    df = df.copy()

    for i in range(1, len(df)):
        # 如果涨跌幅超过30%，用前一日收盘价
        if abs(df.loc[i, 'close'] / df.loc[i-1, 'close'] - 1) > 0.3:
            print(f"修正{df.loc[i, 'trade_date']}的异常数据")
            df.loc[i, 'close'] = df.loc[i-1, 'close']

    return df

df_fixed = fix_outliers(df)
```

### 数据对齐

**问题**：不同股票的交易日期可能不一致（停牌、节假日）

```python
# 示例：三只股票数据日期不对齐
stock1 = ['2024-01-02', '2024-01-03', '2024-01-04']  # 正常交易
stock2 = ['2024-01-02', '2024-01-04']  # 01-03停牌
stock3 = ['2024-01-02', '2024-01-03']  # 01-04停牌
```

**解决方案**：

```python
def align_data(stock_list, start_date, end_date):
    """
    对齐多只股票数据

    参数:
        stock_list: 股票代码列表
        start_date: 开始日期
        end_date: 结束日期
    """
    # 1. 获取所有交易日
    # 从Tushare获取交易日历
    pro = ts.pro_api('你的token')
    trade_cal = pro.trade_cal(
        exchange='SSE',
        start_date=start_date.replace('-', ''),
        end_date=end_date.replace('-', ''),
        is_open='1'
    )
    trade_calendar = pd.to_datetime(trade_cal['cal_date'])

    # 2. 为每只股票创建完整日期索引
    aligned_data = {}

    for stock in stock_list:
        # 获取原始数据
        df = get_stock_data(stock, start_date, end_date)

        # 重新索引到完整交易日历
        df = df.set_index('trade_date')
        df = df.reindex(trade_calendar)

        # 前向填充（停牌期间用前一日数据）
        df = df.fillna(method='ffill')

        aligned_data[stock] = df

    return aligned_data

# 使用
aligned = align_data(['600519', '000858', '600036'], '2024-01-01', '2024-01-31')
```

### 复权处理

**为什么需要复权？**

```
送股、配股、分红会导致价格跳空：
- 10送10：股价瞬间腰斩
- 分红10元：股价下跌10元

不复权的数据无法准确计算收益率！
```

**复权类型**：

```
前复权（qfq）：
- 保持最新价不变
- 调整历史价格
- 适合：看当前市价

后复权（hfq）：⭐ 推荐
- 保持历史价不变
- 调整最新价格
- 适合：回测（计算真实收益率）

不复权：
- 保持原始价格
- 适合：日内交易、实盘下单
```

**示例**：

```python
import tushare as ts
import matplotlib.pyplot as plt

pro = ts.pro_api('你的token')

# 获取不同复权方式的数据
df_no_adjust = pro.daily(ts_code='600519.SH', start_date='20200101', end_date='20241231')
df_qfq = pro.daily(ts_code='600519.SH', start_date='20200101', end_date='20241231', adj='qfq')
df_hfq = pro.daily(ts_code='600519.SH', start_date='20200101', end_date='20241231', adj='hfq')

# 对比
plt.figure(figsize=(12, 6))
plt.plot(df_no_adjust['trade_date'], df_no_adjust['close'], label='不复权')
plt.plot(df_qfq['trade_date'], df_qfq['close'], label='前复权')
plt.plot(df_hfq['trade_date'], df_hfq['close'], label='后复权')
plt.legend()
plt.title('不同复权方式对比')
plt.show()
```

**回测必用后复权**：

```python
# 计算真实收益率（必须用后复权）
df_hfq = df_hfq.sort_values('trade_date')
df_hfq['returns'] = df_hfq['close'].pct_change()

# 累计收益
df_hfq['cumulative_returns'] = (1 + df_hfq['returns']).cumprod() - 1

print(f"总收益率: {df_hfq['cumulative_returns'].iloc[-1]*100:.2f}%")
```

### 停牌数据处理

**停牌的影响**：

```
1. 价格数据缺失
2. 无法买入/卖出
3. 回测时需要特殊处理
```

**处理方法**：

```python
def handle_suspension(df):
    """处理停牌数据"""

    # 1. 标记停牌日期（成交量为0）
    df['is_suspended'] = (df['volume'] == 0)

    # 2. 停牌期间价格用前一日填充
    df['close'] = df['close'].fillna(method='ffill')

    # 3. 添加停牌天数统计
    df['suspension_days'] = df['is_suspended'].groupby(
        (df['is_suspended'] != df['is_suspended'].shift()).cumsum()
    ).cumsum()

    return df

df = handle_suspension(df)

# 回测时的处理
def backtest_with_suspension(df):
    """回测时考虑停牌"""

    for i in range(len(df)):
        if df.loc[i, 'is_suspended']:
            # 停牌日不允许交易
            print(f"{df.loc[i, 'trade_date']}: 停牌，跳过")
            continue

        # 正常交易逻辑
        # ...
```

---

## 14.3 数据存储方案

> 💡 **为什么需要本地存储？**
> 1. 避免重复下载，节省时间
> 2. 减少API调用次数
> 3. 离线也能回测

### CSV文件存储

**最简单的方案**：

```python
import pandas as pd

# 保存
df.to_csv('600519_daily.csv', index=False, encoding='utf-8-sig')

# 读取
df = pd.read_csv('600519_daily.csv')

# 批量保存多只股票
for code in stock_list:
    df = get_stock_data(code)
    df.to_csv(f'data/{code}.csv', index=False)
```

**优缺点**：

```
✅ 优点
- 简单易用
- 可读性好
- 任何工具都能打开

❌ 缺点
- 文件多时管理混乱
- 读取速度慢（大数据量时）
- 占用空间大
```

---

### HDF5高效存储 ⭐

**推荐方案**：适合存储大量时间序列数据

```python
import pandas as pd

# 保存到HDF5
df.to_hdf('stock_data.h5', key='600519', mode='w')

# 批量保存
with pd.HDFStore('all_stocks.h5', mode='w') as store:
    for code in stock_list:
        df = get_stock_data(code)
        store[code] = df

# 读取
df = pd.read_hdf('stock_data.h5', key='600519')

# 读取多只股票
with pd.HDFStore('all_stocks.h5', mode='r') as store:
    df_600519 = store['600519']
    df_000858 = store['000858']

# 查看HDF5文件内容
with pd.HDFStore('all_stocks.h5', mode='r') as store:
    print(store.keys())  # 查看所有key
```

**优缺点**：

```
✅ 优点
- 读写速度快（比CSV快10倍+）
- 压缩存储，节省空间
- 支持随机访问
- 单文件管理，不会文件泛滥

❌ 缺点
- 不能直接打开查看
- 需要pandas读取
```

---

### 数据库存储

**适合大规模数据**：

#### MySQL存储

```python
import pymysql
from sqlalchemy import create_engine
import pandas as pd

# 创建数据库连接
engine = create_engine('mysql+pymysql://用户名:密码@localhost:3306/stock_db')

# 保存到MySQL
df.to_sql('daily_600519', con=engine, if_exists='replace', index=False)

# 读取
df = pd.read_sql('SELECT * FROM daily_600519', con=engine)

# 查询
df = pd.read_sql("""
    SELECT * FROM daily_600519
    WHERE trade_date >= '2024-01-01'
    AND close > 1700
""", con=engine)
```

#### ClickHouse存储（高性能）

```python
from clickhouse_driver import Client

# 连接ClickHouse
client = Client(host='localhost')

# 创建表
client.execute("""
    CREATE TABLE IF NOT EXISTS stock_daily (
        symbol String,
        trade_date Date,
        open Float64,
        high Float64,
        low Float64,
        close Float64,
        volume UInt64
    ) ENGINE = MergeTree()
    ORDER BY (symbol, trade_date)
""")

# 批量插入
data = df.to_dict('records')
client.execute('INSERT INTO stock_daily VALUES', data)

# 查询
result = client.execute("""
    SELECT * FROM stock_daily
    WHERE symbol = '600519'
    AND trade_date >= '2024-01-01'
""")
```

---

### Redis缓存应用

**适合热点数据缓存**：

```python
import redis
import json
import pandas as pd

# 连接Redis
r = redis.Redis(host='localhost', port=6379, db=0)

# 缓存数据
def cache_stock_data(symbol, df):
    """将数据缓存到Redis"""
    key = f"stock:{symbol}:daily"
    value = df.to_json()
    r.setex(key, 3600, value)  # 缓存1小时

# 读取缓存
def get_cached_data(symbol):
    """从Redis读取缓存"""
    key = f"stock:{symbol}:daily"
    value = r.get(key)

    if value:
        return pd.read_json(value)
    else:
        return None

# 使用
cached = get_cached_data('600519')
if cached is not None:
    print("使用缓存数据")
    df = cached
else:
    print("缓存未命中，重新获取")
    df = get_stock_data('600519')
    cache_stock_data('600519', df)
```

---

### 完整数据管理方案

**推荐架构**：

```python
import os
import pandas as pd
import tushare as ts

class DataManager:
    """数据管理器（本地缓存 + 远程数据源）"""

    def __init__(self, tushare_token, cache_dir='./data'):
        self.cache_dir = cache_dir
        self.pro = ts.pro_api(tushare_token)

        # 创建缓存目录
        if not os.path.exists(cache_dir):
            os.makedirs(cache_dir)

    def get_daily(self, symbol, start_date, end_date, use_cache=True):
        """
        获取日线数据（优先使用缓存）
        """
        cache_file = f"{self.cache_dir}/{symbol}.h5"

        # 1. 尝试从缓存读取
        if use_cache and os.path.exists(cache_file):
            print(f"从缓存读取{symbol}")
            df = pd.read_hdf(cache_file, key='daily')

            # 检查日期范围是否满足
            df['trade_date'] = pd.to_datetime(df['trade_date'])
            start = pd.to_datetime(start_date)
            end = pd.to_datetime(end_date)

            if df['trade_date'].min() <= start and df['trade_date'].max() >= end:
                return df[(df['trade_date'] >= start) & (df['trade_date'] <= end)]

        # 2. 缓存不存在或不满足，从远程获取
        print(f"从远程获取{symbol}")
        ts_code = self._convert_code(symbol)
        df = self.pro.daily(
            ts_code=ts_code,
            start_date=start_date.replace('-', ''),
            end_date=end_date.replace('-', ''),
            adj='hfq'
        )

        # 3. 更新缓存
        df.to_hdf(cache_file, key='daily', mode='w')

        return df

    def update_cache(self, symbol):
        """更新缓存到最新日期"""
        cache_file = f"{self.cache_dir}/{symbol}.h5"

        if os.path.exists(cache_file):
            # 读取现有数据
            df_old = pd.read_hdf(cache_file, key='daily')
            last_date = df_old['trade_date'].max()

            # 获取增量数据
            ts_code = self._convert_code(symbol)
            df_new = self.pro.daily(
                ts_code=ts_code,
                start_date=last_date.replace('-', ''),
                adj='hfq'
            )

            # 合并
            df = pd.concat([df_old, df_new]).drop_duplicates('trade_date')
        else:
            # 首次获取
            ts_code = self._convert_code(symbol)
            df = self.pro.daily(
                ts_code=ts_code,
                start_date='20200101',
                adj='hfq'
            )

        # 保存
        df.to_hdf(cache_file, key='daily', mode='w')
        print(f"{symbol}缓存已更新")

    def _convert_code(self, symbol):
        """转换股票代码格式"""
        if symbol.startswith('6'):
            return f"{symbol}.SH"
        else:
            return f"{symbol}.SZ"

# 使用
dm = DataManager(tushare_token='你的token')

# 获取数据（自动使用缓存）
df = dm.get_daily('600519', '2024-01-01', '2024-12-31')

# 定期更新缓存
dm.update_cache('600519')
```

---

## 本章总结

**你已经掌握了**：

```
✅ 两大免费数据源使用
   - Tushare Pro（免费，适合入门）
   - AKShare（完全免费，快速验证）

✅ 数据清洗技术
   - 缺失值处理
   - 异常值检测
   - 数据对齐
   - 复权处理
   - 停牌处理

✅ 数据存储方案
   - CSV（简单）
   - HDF5（推荐）⭐
   - 数据库（大规模）
   - Redis缓存（热点数据）
```

**下一步**：

```
1. 选择一个数据源，获取真实股票数据
2. 实践数据清洗流程
3. 搭建自己的数据管理系统
4. 准备进入第十五章：简单策略实现
```

---

## 练习题

**基础练习**：

```python
# 练习1：使用Tushare获取工商银行（601398）2024年的日线数据
# 你的代码：


# 练习2：检查数据中是否有缺失值，并进行处理
# 你的代码：


# 练习3：计算该股票的5日、10日、20日移动平均线
# 你的代码：


# 练习4：将数据保存到本地CSV文件
# 你的代码：
```

**进阶练习**：

```python
# 练习5：编写函数，批量获取沪深300成分股数据
def get_hs300_data(start_date, end_date):
    # 你的代码：
    pass


# 练习6：编写DataManager类，实现数据缓存机制
class DataManager:
    def __init__(self, tushare_token, cache_dir):
        # 你的代码：
        pass

    def get_data(self, symbol, start_date, end_date):
        # 你的代码：
        pass


# 练习7：检测并处理数据中的异常值
def clean_data(df):
    # 你的代码：
    pass
```

---

## 常见问题FAQ

**Q1: 免费数据够用吗？**

```
学习阶段：够用
- Tushare日线数据免费
- AKShare完全免费

实盘阶段：建议付费
- 数据质量更稳定
- 分钟级数据
- 更全面的财务数据
```

**Q2: 数据要存多少年？**

```
建议：至少5年

原因：
- 回测需要足够长的历史
- 验证策略在不同市场环境下的表现
- 计算长期技术指标（如200日均线）

但注意：
- 市场环境变化，太久远的数据可能失效
- 10年以上的数据参考价值降低
```

**Q3: 后复权和前复权到底用哪个？**

```
回测：必须用后复权（hfq）
- 计算真实收益率
- 反映真实投资回报

实盘：用不复权或前复权
- 下单用实际价格
- 前复权方便看当前市价

记住：回测用后复权！
```

**Q4: 数据存储用哪种方案？**

```
小规模（<100只股票）：HDF5 ⭐
中规模（100-1000只）：HDF5 或 MySQL
大规模（>1000只）：ClickHouse + HDF5

个人量化：HDF5足够
```

**Q5: 如何验证数据质量？**

```
1. 交叉验证
   - 对比多个数据源
   - 看是否一致

2. 逻辑检查
   - 最高价 >= 最低价
   - 成交量 >= 0
   - 涨跌停限制

3. 对比公开数据
   - 与东方财富、同花顺对比
   - 重点关注重要日期（分红、送股）
```

**Q6: Tushare积分不够怎么办？**

```
1. 每日签到（+1积分）
2. 邀请好友（+100积分）
3. 充值购买
4. 降低需求（只用日线数据）
5. 使用AKShare替代
```

---

## 学习资源推荐

**数据源官方文档**：

```
Tushare Pro: https://tushare.pro/document/2
AKShare: https://akshare.akfamily.xyz/
```

**Pandas官方文档**：

```
https://pandas.pydata.org/docs/
```

**相关书籍**：

```
《利用Python进行数据分析》- Wes McKinney
《Python金融大数据分析》- Yves Hilpisch
```

---

**恭喜！你已经掌握量化交易的数据基础。**

**下一章预告**：第十五章《简单策略实现》，我们将：
- 实现第一个完整的双均线策略
- 学习技术指标计算
- 进行简单回测
- 分析策略表现

**数据已备齐，策略开发正式开始！** 🚀
